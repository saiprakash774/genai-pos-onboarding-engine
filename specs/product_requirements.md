# Core Business Rules

- The system must forward-fill missing parent data in spreadsheets.
- Modifiers must be mapped using an 'Applies_To' logic (e.g., Cold Foam only applies to Cold Coffees).
- The final output must be a strict JSON schema, not plain text.
